import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // InternReach runs on port 4001 (isolated from other VPS projects)
  // Start with: PORT=4001 npm start
};

export default nextConfig;
